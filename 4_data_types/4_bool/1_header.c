// Program to implement boolean data type using header file

#include <stdio.h>
#include <stdbool.h>

// main function
int main()
{
    // Boolean data types declared
    bool a = true;
    bool b = false;

    // Print the boolean values as integers (1 for true and 0 for false)
    printf("a = %d\n", a);
    printf("b = %d\n", b);

    return 0;
}